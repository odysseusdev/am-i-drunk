import { MantineThemeOverride } from "@mantine/core";

const theme: MantineThemeOverride = {
	colorScheme: "light",
	primaryColor: "cyan",
	defaultGradient: { from: "yellow", to: "pink", deg: 45 },
};

export default theme;
